machine:
  environment:
    PYTHONDONTWRITEBYTECODE: '1'

dependencies:
   pre:
     - pip install flake8
     - bundle install
     - npm install -g dredd@unstable

test:
   pre:
     - python setup.py flake8
   post:
     - bundle exec cucumber
